{
  "name": "version-test",
  "version": "5.2.3",
  "description": "for testing version bumps, including a NPM script to bump version in WP plugin files",
  "main": "index.js",
  "main_file": "fake_plugin.php",
  "scripts": {
    "preversion": "echo is this it? %s",
    "version": "./vars.js && sed \"s/^Version:.*/Version: $npm_package_version/g\" $npm_package_main_file",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "joe maller <joe@ideasonpurpose.com>",
  "license": "MIT"
}
