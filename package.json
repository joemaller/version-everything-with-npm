{
  "name": "version-test",
  "version": "1.1.1",
  "description": "for testing version bumps, including a NPM script to bump version in WP plugin files",
  "main": "index.js",
  "scripts": {
    "preversion": "./vars.js",
    "version": "./vars.js && sed \"s/^Version:.*/Version: $npm_package_version/g\" fake_plugin.php",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "joe maller <joe@ideasonpurpose.com>",
  "license": "MIT"
}
